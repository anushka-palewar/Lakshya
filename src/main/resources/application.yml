spring:
  application:
    name: DreamWeb
  
  datasource:
    url: jdbc:postgresql://localhost:5432/dreamweb_db
    username: postgres
    password: Lisha@15
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970 # 256-bit secret
  expiration: 86400000 # 24 hours in milliseconds

# API Keys for image services (optional - will use fallback images if not configured)
unsplash:
  access:
    key: "" # Add your Unsplash API key here if needed

openai:
  api:
    key: "" # Add your OpenAI API key here if needed

# Logging configuration
logging:
  level:
    root: INFO
    sp.main.DreamWeb: DEBUG
    org.springframework.security: DEBUG

